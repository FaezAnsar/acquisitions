# Production Environment Configuration
# Server config
PORT=3000
NODE_ENV=production
LogLevel=info

# Database config - Neon Cloud Database
# This should be injected via environment variables in production deployment
# Example: DATABASE_URL=postgresql://username:password@ep-xxxxx.us-east-1.aws.neon.tech/dbname?sslmode=require
DATABASE_URL=${DATABASE_URL}

# Arcjet - Production key should be injected via environment variables
ARCJET_KEY=${ARCJET_KEY}